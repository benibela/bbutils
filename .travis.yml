language: generic
sudo: required
dist: trusty

os:
  - linux

install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq -y fpc

script:
  - fpc -O2 tests/tests_bbutils_only.lpr 
  - tests/tests_bbutils_only

notifications:
  email:
    on_success: false
    on_failure: change